@model WebApplication1.Models.Carro

@{
    ViewBag.Title = "Exibir";
    var linkEditar = Url.Action("Editar", new { id = ViewBag.Id });
    var linkVoltar = Url.Action("Listar");
}

<div class="detail-container">
    <div class="detail-header">
        <div class="detail-icon">
            <i class="bi bi-car-front"></i>
        </div>
        <h1 class="detail-title">Detalhes do Carro</h1>
        <div class="detail-subtitle">Informações cadastrais</div>
    </div>

    <div class="detail-card">
        <dl class="detail-list">
            <div class="detail-item">
                <dt>@Html.DisplayNameFor(model => model.Placa)</dt>
                <dd>@Html.DisplayFor(model => model.Placa)</dd>
            </div>

            <div class="detail-item">
                <dt>@Html.DisplayNameFor(model => model.Cor)</dt>
                <dd>@Html.DisplayFor(model => model.Cor)</dd>
            </div>

            <div class="detail-item">
                <dt>@Html.DisplayNameFor(model => model.DataFabricacao)</dt>
                <dd>@Html.DisplayFor(model => model.DataFabricacao)</dd>
            </div>
        </dl>
    </div>

    <div class="detail-actions">
        <a href="@linkVoltar" class="button-btn-secondary">
            <i class="bi bi-arrow-left"></i> Voltar
        </a>
        <a href="@linkEditar" class="button-btn-primary">
            <i class="bi bi-pencil-square"></i> Editar
        </a>
    </div>
</div>
